定义一个三维坐标点的类，用友元函数实现“++”运算符的重载，给三位坐标的每一个维度加一。用成员函数重载运算符“+”，用来实现两个三位坐标点类的相加操作。用友元函数实现“<<”的重载，用于自定义格式化输出。类需要有一个带三个参数（x，y，z）的构造函数，参数的默认值都为0，成员变量均为私有类型。
注：全部使用int
输入格式：（point1和point2，数字之间用空格隔开，输入完成之后回车）
1 2 3 4 5 6

输出格式：(point1自加1，之后和point2相加输出，“,“”:”皆为英文符号)
x:2,y:3,z:4（换行符）
x:6,y:8,z:10（无换行符）