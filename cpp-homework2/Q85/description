输入一个数字数组nums，长度为n（n<100）。定义4种命令：replace、move、translate，print。  
replace命令将数组中所有的的数字a替换为数字b，格式为replace a b,a为被替换的数字，b为替换的数字。  
move命令将指定数字a全部移动到数组开头或末尾，命令格式为move head(tail) a，a为被移动的数字。  
translate命令将数组整体向前（或后）平移，尾部越界的数字补全到数组开头，开头越界的数字补全到数组末尾，命令格式为translate head(tail) a，a为移动的格数。  
print命令要求输出当前的数组信息。  
首先输入n，表示数组大小，接着n个输入为具体数组。然后输入m，表示命令数量，接着输入m条命令。将命令的结果进行输出。

Input：  
5  
0 1 0 3 12  
7  
print  
replace 1 3  
print  
move tail 3  
print  
translate head 2   
print  

Output：  
0 1 0 3 12  
0 3 0 3 12  
0 0 12 3 3  
12 3 3 0 0 