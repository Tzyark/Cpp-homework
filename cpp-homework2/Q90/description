自定义一个链表的结构，不能使用已有库
定义一些链表的操作ADD，REVERSE，REMOVEREPEAT，DEL，PRINT，SIZE
ADD：往链表追加一个元素，如果之前一个元素都没有，则该元素作为链表的第一个元素
	如： ADD 1，将1添加至链表
REVERSE：将链表反转
REMOVEREPEAT：剔除链表中重复的元素，保留一个，如 REMOVEREPEAT n，则剔除链表中
			所有值为n的元素，保留最后一个,如果没有该元素，则不操作
DEL：删除链表中的所有指定的元素，如 DEL 1，删除链表中值为1的所有元素，不存在元素则不进行任何操作
PRINT：将链表从第一个依次输出,如果为空，则输出“NULL”
SIZE：输出链表的元素个数

输入：
先输入整数n，表示将会有n条指令输入
然后依次输入n条指令（指令只会在给定的指令范围内）

输出：
根据指令的要求，如果指令要求输出，则将结果输出到控制台（注意最后一次输出没有回车）

注：此处的“NULL”输出时都表示字符串

示例：
输入

12
ADD 1
ADD 1
ADD 2
ADD 1
SIZE
PRINT
REMOVEREPEAT 1
PRINT
ADD 1
PRINT
DEL 1
PRINT

输出：
4
1 1 2 1
2 1
2 1 1
2（没有回车）
