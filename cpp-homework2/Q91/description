输入学生信息（包括姓名，学号，科目，成绩），学号为唯一，成绩可能发生更新，若信息中缺少某个学生的某个科目，则将该学生的该科目得分计算为0。
将（每个学生各科成绩的平均得分）小于等于（全部学生各科成绩的平均分）的学生按平均分从高到低进行排序,并输出学号。同平均分学生按学号从高到低排序。
Input： n
		name，id，subject，score
		……
Output：id1
		id2
		id3(无换行)


示例：
Input: 5
		student1 1 cpp 70
		student1 1 java 80
		student1 1 cpp 80
		student2 2 cpp 70
		student3 3 java 90
Output：3
		2(无换行)